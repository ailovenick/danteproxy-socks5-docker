# SOCKS5 Proxy Server with Authentication

Простой SOCKS5 прокси-сервер на базе Dante, работающий в Docker. Поддерживает аутентификацию по логину и паролю.

## 📋 Требования

*   Docker
*   Docker Compose

## 🚀 Инструкция: Установка и Управление пользователями

Вся настройка пользователей происходит через файл `.env`. Пароли передаются при запуске и **не сохраняются** в истории образа Docker.

### 1. Настройка пользователей
Создайте или откройте файл `.env`. Запишите туда пользователей в переменную `PROXY_USERS`.
Формат: `логин:пароль`. Разделяйте пользователей пробелом.

**Пример `.env` (3 пользователя):**
```env
PROXY_USERS="user1:pass1 user2:pass2 user3:pass3"
```

### 2. Запуск и Применение изменений
Чтобы запустить сервер **или** применить изменения в списке пользователей (добавление/удаление/смена пароля), выполните команду перезапуска:

```bash
docker-compose up -d
```
*Контейнер пересоздастся, прочитает новый `.env` и обновит список пользователей.*

Сервер работает на порту **62335**.

## 🛠 Проверка работы

Вы можете проверить работу прокси с помощью `curl`:

```bash
curl -v -x socks5://user1:pass1@localhost:62335 google.com
```

## 📂 Структура файлов

*   `docker-compose.yml` — описание сервиса, проброс портов и переменных.
*   `Dockerfile` — инструкции сборки образа.
*   `entrypoint.sh` — скрипт инициализации, создающий пользователей при старте.
*   `sockd.conf` — конфигурация Dante сервера.
*   `.env` — **СЕКРЕТНЫЙ** файл со списком пользователей и паролей.
