# SOCKS5 Proxy Server with Authentication

Простой SOCKS5 прокси-сервер на базе Dante, работающий в Docker. Поддерживает аутентификацию по логину и паролю.

## 📋 Требования

*   Docker
*   Docker Compose

## 🚀 Инструкция: Установка и Управление пользователями

Процесс первого запуска и любого изменения пользователей (добавление, удаление, смена пароля) **одинаковый**:

1.  **Настройте пользователей:**
    Откройте файл `.env`. Отредактируйте переменную `PROXY_USERS`.
    Формат: `пользователь:пароль`. Разделяйте пользователей пробелом.

    Пример `.env`:
    ```env
    PROXY_USERS="user1:pass1 user2:pass2 user3:pass3"
    ```

2.  **Запустите или Обновите сервер:**
    Выполните команду (она и запустит сервер, и применит изменения):
    ```bash
    docker-compose up -d --build
    ```
    *Флаг `--build` обязателен при любых изменениях в `.env`, чтобы новые пользователи добавились в систему.*

    Сервер работает на порту **62335**.

## 🛠 Проверка работы

Вы можете проверить работу прокси с помощью `curl`:

```bash
curl -v -x socks5://user1:pass1@localhost:62335 google.com
```

Если всё работает, команда вернет ваш внешний IP адрес.

## 📂 Структура файлов

*   `docker-compose.yml` — описание сервиса и проброс портов.
*   `Dockerfile` — инструкции сборки образа, создание пользователей.
*   `sockd.conf` — конфигурация Dante сервера.
*   `.env` — **СЕКРЕТНЫЙ** файл со списком пользователей и паролей (не добавляйте в публичный репозиторий!).
